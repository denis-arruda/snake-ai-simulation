quarkus.application.name=snake-engine
quarkus.http.port=8080

# JIB container image (build with: mvn package -Dquarkus.container-image.build=true)
quarkus.container-image.group=snake
quarkus.container-image.name=snake-engine
quarkus.container-image.tag=latest
quarkus.jib.base-jvm-image=eclipse-temurin:26-jre

# Pulsar
mp.messaging.connector.smallrye-pulsar.serviceUrl=${PULSAR_SERVICE_URL:pulsar://localhost:6650}

mp.messaging.outgoing.render-state.connector=smallrye-pulsar
mp.messaging.outgoing.render-state.topic=persistent://public/default/snake-render-state

mp.messaging.incoming.agent-decisions.connector=smallrye-pulsar
mp.messaging.incoming.agent-decisions.topic=persistent://public/default/snake-agent-decisions

# OTel -> Tempo
quarkus.otel.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://lgtm:4317}
quarkus.otel.exporter.otlp.protocol=grpc

# Prometheus metrics at /q/metrics
quarkus.micrometer.export.prometheus.enabled=true

# Game settings
game.tick.interval=200ms
game.grid.size=30
game.food.max=5
game.snake.initial-length=3
